<form method="post" action="{{ path('app_to_do_list_delete', {'id': to_do_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ to_do_list.id) }}">
    <button class="btn">Delete</button>
</form>
