<form method="post" action="{{ path('app_itinerary_delete', {'id': itinerary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ itinerary.id) }}">
    <button class="btn">Delete</button>
</form>
